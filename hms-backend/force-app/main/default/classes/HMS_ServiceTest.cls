@isTest
private class HMS_ServiceTest {
    // This class mostly tests legacy methods that were refactored/removed.
    // getPatientTimeline is covered in HMS_TimelineTest.
    // Keeping this placeholder or we can delete it.
    @isTest 
    static void testPlaceholder() {
        Assert.isTrue(true);
    }
}
