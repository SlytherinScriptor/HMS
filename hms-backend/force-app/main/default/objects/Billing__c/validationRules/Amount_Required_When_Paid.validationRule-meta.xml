<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Amount_Required_When_Paid</fullName>
    <active>true</active>
    <description>Total Amount is required when Status is set to Paid.</description>
    <errorConditionFormula>AND(
    ISPICKVAL(Status__c, 'Paid'),
    OR(
        ISNULL(Total_Amount__c),
        Total_Amount__c &lt;= 0
    )
)</errorConditionFormula>
    <errorMessage>Total Amount must be greater than zero when Status is Paid.</errorMessage>
</ValidationRule>
